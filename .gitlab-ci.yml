# .gitlab-ci.yml (snippet)
image: docker:27.3.1

variables:
  DOCKER_BUILDKIT: "1"
  DOCKERFILE_DIR: "src/cro_improvement_feature/network_analyzer"
  ECR_REGISTRY: "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com"

before_script:
  # install prerequisites
  - apk add --no-cache curl unzip jq
  # download & install AWS CLI v2
  - curl -fsSL "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o awscliv2.zip
  - unzip -q awscliv2.zip
  - ./aws/install --bin-dir /usr/local/bin --install-dir /usr/local/aws-cli
  - aws --version
  # ECR login
  - aws ecr get-login-password --region "$AWS_DEFAULT_REGION" \
    | docker login --username AWS --password-stdin "$ECR_REGISTRY"
  
build-and-push:
  stage: build
  tags: ["docker"]
  script:
    - export IMAGE="$ECR_REGISTRY/$ECR_REPOSITORY"
    - export TAG="${CI_COMMIT_SHORT_SHA}"
    - docker build -t "$IMAGE:$TAG" -f "$DOCKERFILE_DIR/Dockerfile.GitLab" "$DOCKERFILE_DIR"
    - docker push "$IMAGE:$TAG"

